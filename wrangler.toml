name = "qwen3-tts-webui"
compatibility_date = "2025-01-01"

# Serve ./dist as static assets — no Worker script needed.
# In wrangler 3.x, just specify the directory; no binding required for
# assets-only Workers (binding is only needed if you fetch assets from JS).
[assets]
directory = "./dist"
